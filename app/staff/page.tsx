'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';export default function Page(){const r=useRouter(),[s,setS]=useState<any>(null);useEffect(()=>{fetch('/api/staff/me',{cache:'no-store'}).then(async x=>{if(x.status===401){r.replace('/staff/login');return;}const d=await x.json();if(x.ok)setS(d.staff);})},[r]);if(!s)return <main style={{padding:40,fontFamily:'system-ui'}}>Loading LAUREM Staff Portal…</main>;return <main style={{minHeight:'100vh',background:'#f4f7fb',fontFamily:'system-ui',padding:24,color:'#102a43'}}><div style={{maxWidth:1050,margin:'0 auto'}}><div style={{background:'#fff',border:'1px solid #e5eaf0',borderRadius:18,padding:28}}><div style={{color:'#0f766e',fontSize:12,fontWeight:900,letterSpacing:1.4}}>LAUREM CARE</div><h1>Welcome, {s.full_name}</h1><p style={{color:'#627d98'}}>LAUREM ID: <strong>{s.laurem_id||s.employee_number}</strong></p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(200px,1fr))',gap:12,marginTop:20}}>{[['Messages','/staff/messages'],['My Shifts','/staff/shifts'],['Timesheets','/staff/timesheets'],['Documents','/staff/documents']].map(([a,b])=><button key={b} onClick={()=>r.push(b)} style={{padding:18,textAlign:'left',background:'#fff',border:'1px solid #dbe5ea',borderRadius:12,fontWeight:900}}>{a}<div style={{fontSize:12,color:'#627d98',marginTop:6}}>Open →</div></button>)}</div></div></div></main>}
