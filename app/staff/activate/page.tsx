'use client';
import{useState}from'react';import{useSearchParams,useRouter}from'next/navigation';
export default function Page(){const q=useSearchParams(),r=useRouter(),token=q.get('token')||'',email=q.get('email')||'';const[p,setP]=useState(''),[e,setE]=useState(''),[busy,setBusy]=useState(false);async function submit(ev:React.FormEvent){ev.preventDefault();setBusy(true);const res=await fetch('/api/staff/auth/activate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,email,password:p})});const d=await res.json();if(!res.ok){setE(d.error||'Unable to activate portal.');setBusy(false);return;}r.replace('/staff');}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f4f7fb',fontFamily:'system-ui',padding:20}}><form onSubmit={submit} style={{background:'#fff',padding:28,border:'1px solid #e5eaf0',borderRadius:18,width:'min(440px,100%)'}}><div style={{color:'#0f766e',fontSize:12,fontWeight:900,letterSpacing:1.4}}>LAUREM CARE</div><h1>Activate your personal portal</h1><p style={{color:'#627d98'}}>Create a password and your staff account will be activated.</p><input readOnly value={email} style={{width:'100%',boxSizing:'border-box',padding:11,border:'1px solid #cbd5e1',borderRadius:10,marginBottom:10}}/><input type="password" minLength={10} required placeholder="Password (10+ characters)" value={p} onChange={x=>setP(x.target.value)} style={{width:'100%',boxSizing:'border-box',padding:11,border:'1px solid #cbd5e1',borderRadius:10}}/><button disabled={busy} style={{width:'100%',marginTop:12,padding:12,border:0,borderRadius:10,background:'#0f766e',color:'#fff',fontWeight:900}}>{busy?'Activating…':'Activate portal'}</button>{e&&<p style={{color:'#b42318'}}>{e}</p>}</form></main>}
