'use client';
import{useState}from'react';import{useRouter}from'next/navigation';export default function Page(){const r=useRouter(),[id,setId]=useState(''),[p,setP]=useState(''),[e,setE]=useState(''),[b,setB]=useState(false);async function go(ev:React.FormEvent){ev.preventDefault();setB(true);const x=await fetch('/api/staff/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({laurem_id:id,password:p})}),d=await x.json();if(!x.ok){setE(d.error||'Unable to sign in.');setB(false);return;}r.replace('/staff');}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f4f7fb',fontFamily:'system-ui'}}><form onSubmit={go} style={{background:'#fff',padding:28,borderRadius:18,border:'1px solid #e5eaf0',width:'min(440px,90%)'}}><div style={{fontSize:12,fontWeight:900,color:'#0f766e',letterSpacing:1.4}}>LAUREM CARE</div><h1>Staff Portal</h1><input required placeholder="LAU-000001" value={id} onChange={x=>setId(x.target.value)} style={{width:'100%',boxSizing:'border-box',padding:11,border:'1px solid #cbd5e1',borderRadius:10,marginBottom:10}}/><input required type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} style={{width:'100%',boxSizing:'border-box',padding:11,border:'1px solid #cbd5e1',borderRadius:10}}/><button disabled={b} style={{width:'100%',marginTop:12,padding:12,border:0,borderRadius:10,background:'#0f766e',color:'#fff',fontWeight:900}}>{b?'Signing in…':'Sign in'}</button>{e&&<p style={{color:'#b42318'}}>{e}</p>}</form></main>}
