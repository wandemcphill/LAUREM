# LAUREM Portal lifecycle

The canonical lifecycle is application -> accepted contract -> readiness -> staff profile -> portal provisioning -> activation. Business rules belong in `lib/laurem-lifecycle.ts` and should be reused by entry points rather than duplicated.
